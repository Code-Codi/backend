package com.codiapp.codi.domain.brief.repository;

import com.codiapp.codi.domain.brief.entity.BriefDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BriefDetailRepository extends JpaRepository<BriefDetail, Long> {
}
